# Experimental / historical scripts

Optional offline data utilities. The **supported ML workflow** lives in **`ml/train_predict_vorp.py`** (see main README).

The live app still does **not** train models per HTTP request: it **merges** `data/ml_player_scores.csv` produced by that script.
